server:
  port: 8080

app:
  environment:
    name: DEV
  frontend:
    url: http://localhost:4200
  websocket:
    allowed-origins: "http://localhost:4200,http://localhost:1025,http://localhost:8080"
